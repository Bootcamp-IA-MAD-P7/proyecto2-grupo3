# Daily Scrum - 28/05/2026

## Sprint
Sprint 1 - MVP Esencial

## Resumen del equipo
El equipo centró el trabajo en desbloquear la conexión real con base de datos para poder continuar con los endpoints CRUD del MVP esencial.

Se decidió utilizar Supabase como proveedor de PostgreSQL gestionado, manteniendo PostgreSQL como base de datos relacional del proyecto y facilitando que todo el equipo trabaje contra una misma base de datos.

## Qué hicimos ayer
- Revisamos el estado técnico del repositorio.
- Identificamos ramas activas que no debían fusionarse directamente.
- Confirmamos que el backend tenía FastAPI, Swagger, endpoint `/health`, modelos, schemas, logging y manejo inicial de errores.
- Revisamos que la historia de API REST seguía pendiente principalmente de CRUD y validación real de base de datos.
- Actualizamos documentación del README con el estado del backend y la base de datos pendiente.

## Qué haremos hoy
- Documentar la daily del día.
- Documentar el checkpoint técnico realizado.
- Revisar y actualizar Jira con el estado real del Sprint 1.
- Configurar Supabase como base de datos PostgreSQL del proyecto.
- Obtener y validar la cadena de conexión `DATABASE_URL`.
- Confirmar que el backend puede conectarse a Supabase mediante SQLAlchemy.
- Comprobar que el backend puede consultar las tablas existentes.
- Dejar documentada la decisión académica sobre la configuración compartida de entorno.
- Continuar con la implementación de endpoints CRUD básicos, empezando por `salas`.

## Avances realizados
- El equipo accedió al proyecto de Supabase.
- Se obtuvo la cadena de conexión de Supabase mediante Session Pooler.
- Se configuró `backend/.env` con la conexión real.
- Se actualizó `backend/.env.example` como plantilla.
- Se validó la conexión del backend con Supabase.
- Se comprobó que SQLAlchemy puede conectar correctamente con la base de datos.
- Se acordó incluir temporalmente `backend/.env` en el repositorio por motivos académicos y de coordinación del equipo.
- Se documentó en el README que esta decisión no representa una buena práctica profesional y que se recomienda rotar la contraseña al finalizar el proyecto.
- Se creó y cerró una tarea técnica en Jira para reflejar la documentación y decisión sobre Supabase/configuración compartida.

## Bloqueos
- No hay bloqueo técnico de conexión a base de datos en este momento.
- Queda pendiente implementar y probar los endpoints CRUD contra Supabase.
- Queda pendiente revisar Swagger cuando los endpoints CRUD estén integrados.

## Próximos pasos
- Fusionar la configuración compartida si el equipo la valida.
- Crear o revisar la documentación del checkpoint técnico.
- Actualizar Jira con estados definitivos de las subtareas relacionadas.
- Implementar CRUD de `salas`.
- Probar CRUD en Swagger.
- Continuar con CRUD de `clientes`, `sesiones` y `reservas`.
- Preparar tests iniciales para los endpoints principales.

## Notas
La conexión con Supabase queda validada y deja desbloqueado el desarrollo del CRUD. La prioridad del resto del día es que la aplicación empiece a mostrar funcionalidad real en Swagger más allá del endpoint `/health`.
